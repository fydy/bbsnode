language: node_js

node_js: stable

sudo: true

before_install:
  - git config --global user.name "fydy"
  - git config --global user.email "barberw@mail.lcc.edu"
  - npm install
  - npm install ghost-cloudinary-storage
  - cp -r node_modules/ghost-cloudinary-storage content/adapters/storage/ghost-cloudinary-storage

install:
  - npm update
  
script: 
  - echo "npm test temporarily disabled"
  
after_success:
  - git add --all .
  - git commit -m "Travis CI Auto Builder To Heroku"
  - git push --quiet --force https://$ghost@github.com/fydy/bbsnode.git ghost
