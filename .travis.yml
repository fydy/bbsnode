language: node_js
# When changing node version also update it on lines 31 and 49.
node_js:
  - "10"

sudo: false

cache:
  directories:
    - node_modules

before_install:
  - travis encrypt $(heroku auth:token) --add deploy.api_key
  - heroku git:clone -a bbsnode

install:
- node app --setup
